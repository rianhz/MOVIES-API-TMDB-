import React from 'react';
import { Container } from 'react-bootstrap';

const KontenOne = ({ id }) => {
  return (
    <Container style={{ height: '100vh' }} id={id}>
      KontenOne
    </Container>
  );
};

export default KontenOne;
