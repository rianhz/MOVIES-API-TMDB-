import React from 'react';
import { Container } from 'react-bootstrap';

const KontenTiga = ({ id }) => {
  return (
    <Container style={{ height: '100vh' }} id={id}>
      kontenTiga
    </Container>
  );
};

export default KontenTiga;
